train.epochs = 201
train.batch_size = 256
train.learning_rate = 0.0001
train.weight_decay = 0.01
train.gradient_accumulate_every = 1

train.dataset_folder = "datasets/mind"

train.vae_input_dim = 768
train.vae_hidden_dims = [512, 256]
train.vae_embed_dim = 128

train.vae_num_categories = 18
train.vae_c2_codebook_size = 128
train.vae_c3_codebook_size = 512

train.vae_codebook_normalize = False
train.vae_sim_vq = False
train.vae_codebook_mode = %QuantizeForwardMode.GUMBEL_SOFTMAX

train.lambda_rec = 1.0
train.lambda_cb = 1.0
train.lambda_com = 0.25

train.kmeans_encode_batch_size = 512
train.kmeans_n_init = 10

train.gumbel_t0 = 1.0
train.gumbel_min_t = 0.1
train.gumbel_anneal_rate = 0.000058

train.split_batches = True
train.amp = True
train.mixed_precision_type = "fp16"
train.num_workers = 0

# Validation
# 학습 전/후 Train + Validation loss를 모두 출력하려면 True
train.do_eval = True
train.eval_every_epochs = 10


# Early Stopping
# MIND는 현재 max epoch가 201로 짧고 별도 실험 이력이 있으므로 기본은 OFF.
# 필요하면 dataset별 learning curve 확인 후 별도로 켠다.
train.early_stopping = False
train.early_stopping_min_epochs = 20
train.early_stopping_patience = 10
train.early_stopping_min_delta = 0.001

train.pretrained_rqvae_path = None
train.save_dir_root = "out/rqvae/mind/"
train.save_model_every_epochs = 10

# Logging
train.wandb_logging = False
train.wandb_project = "news-rqvae-training"


train.seed = 42
